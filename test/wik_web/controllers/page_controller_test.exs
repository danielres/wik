defmodule WikWeb.PageControllerTest do
  use WikWeb.ConnCase

  # test "GET /", %{conn: conn} do
  #   conn = get(conn, ~p"/")
  #   assert html_response(conn, 200) =~ "data-telegram-login"
  # end
end
